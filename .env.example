# 这个客户端不再直接持有大模型 key——AI 调用统一转发到自建服务器
# （server/ 目录），真实 key 只在服务器的环境变量里。这样做是因为这个 key
# 之前直接编译进分发出去的客户端安装包里，被人拆出来盗刷过一次真实发生
# 的事故（详见 src-tauri/src/remote.rs 顶部注释）。
#
# 如果你要部署/维护那台服务器，配置项在 server/deploy/.env.example，
# 不是这个文件。这个文件目前没有客户端需要设置的环境变量。
