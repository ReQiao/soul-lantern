# AI 模式用的大模型 API key。任何 OpenAI 兼容接口都可以（通义千问 DashScope、OpenAI 官方，
# 或其他兼容服务）——服务商、接口地址、模型名都在应用界面里选/填，这里只是免界面配置的兜底。
# 也可以不设这里，直接在界面的「API Key」输入框里填——优先级：界面输入 > 环境变量。
AI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 以下为可选覆盖项，一般不用改（界面上选好服务商后会覆盖这里）。
# AI_ENDPOINT=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
# AI_MODEL=qwen-plus
